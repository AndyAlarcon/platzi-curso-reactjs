const Card = ({title, description}) => {
    return (
        <section>
            <h1>{title}</h1>
            <description>{description}</description>
        </section>
    )
}
export default Card;